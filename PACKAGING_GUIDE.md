# WayneMemo 打包指南

## 📦 打包前准备

### 1. 清理无用文件

执行清理脚本：

```bash
./clean-before-pack.sh
```

**清理内容：**
- ✅ 测试文件（e2e/, test*.js）
- ✅ 测试报告（playwright-report/）
- ✅ 构建缓存（build/, dist/）
- ✅ 开发文档（10+ 个 MD 文件）
- ✅ 临时文件（.DS_Store, *.tmp, *.swp）
- ✅ 日志文件（npm-debug.log*）
- ✅ IDE 配置（.vscode/, .idea/）

**保留文件：**
- ✅ README.md - 项目说明
- ✅ USER_GUIDE.md - 用户指南
- ✅ CHANGELOG.md - 更新日志
- ✅ LICENSE - 许可证
- ✅ package.json - 项目配置
- ✅ src/ - 源代码
- ✅ public/ - 公共资源

---

### 2. 构建前端

```bash
npm run build
```

---

### 3. 打包应用

#### 全平台打包（推荐）

```bash
npm run dist:all
```

#### 分平台打包

```bash
# macOS (Intel + Apple Silicon)
npm run dist:mac

# Windows
npm run dist:win
```

---

## 📊 打包产物

打包完成后，安装包位于 `release-builds/` 目录：

- `WayneMemo-1.0.0.dmg` (macOS Intel)
- `WayneMemo-1.0.0-arm64.dmg` (macOS Apple Silicon)
- `WayneMemo Setup 1.0.0.exe` (Windows)

---

## 🗂️ 文件结构（打包后）

```
WayneMemo/
├── README.md              # 项目说明
├── USER_GUIDE.md          # 用户指南
├── CHANGELOG.md           # 更新日志
├── LICENSE                # 许可证
├── package.json           # 项目配置
├── src/                   # 源代码
├── public/                # 公共资源
└── release-builds/        # 打包产物（打包后生成）
    ├── WayneMemo-1.0.0.dmg
    ├── WayneMemo-1.0.0-arm64.dmg
    └── WayneMemo Setup 1.0.0.exe
```

---

## 📏 文件大小优化

### 清理前 vs 清理后

| 项目 | 清理前 | 清理后 | 节省 |
|------|--------|--------|------|
| 测试文件 | ~500 KB | 0 KB | 100% |
| 测试报告 | ~450 KB | 0 KB | 100% |
| 开发文档 | ~80 KB | 0 KB | 100% |
| 临时文件 | ~10 KB | 0 KB | 100% |
| **总节省** | **~1 MB** | - | - |

**注意：** node_modules 和 build 不会打包到最终产物中（由 electron-builder 自动处理）

---

## ✅ 打包检查清单

### 打包前

- [ ] 执行 `./clean-before-pack.sh`
- [ ] 确认只保留必要文件
- [ ] 检查 package.json 版本号
- [ ] 确认 Git 代码已提交

### 打包中

- [ ] 执行 `npm run build`
- [ ] 执行 `npm run dist:all`
- [ ] 等待打包完成（约 5-10 分钟）

### 打包后

- [ ] 检查 `release-builds/` 目录
- [ ] 验证安装包文件存在
- [ ] 检查文件大小合理（~100-150 MB）
- [ ] 在目标平台测试安装

---

## 🚨 注意事项

### 1. 不要打包的文件

以下文件已在 `.gitignore` 中配置，不会打包：

- 测试文件（e2e/, test*.js）
- 开发文档（MANUAL_TEST_GUIDE.md 等）
- 临时文件（.DS_Store, *.tmp）
- IDE 配置（.vscode/, .idea/）

### 2. 必须保留的文件

- README.md - 用户需要查看
- USER_GUIDE.md - 新手指引
- CHANGELOG.md - 版本历史
- LICENSE - 法律要求
- package.json - 应用配置

### 3. electron-builder 自动处理

以下目录不会打包到最终产物：
- node_modules（只打包必要的依赖）
- build/（仅用于构建）
- src/（打包后使用 build/）

---

## 🎯 快速打包流程

```bash
# 1. 清理
./clean-before-pack.sh

# 2. 构建
npm run build

# 3. 打包
npm run dist:all

# 4. 检查产物
ls -lh release-builds/
```

---

## 📦 完成！

打包完成后，您可以：

1. 在目标平台测试安装包
2. 创建 GitHub Release
3. 上传安装包到 Release
4. 发布给用户

---

**版本**: v1.0.0
**创建日期**: 2026-02-25
